import tkinter as tk

window = tk.Tk()

frame = tk.Frame(window)
tk.Label(frame, text = "Horizontal lab1").grid(row = 0, column = 0)
tk.Label(frame, text = "Horizontal lab2").grid(row = 0, column = 1)
frame.pack()

window.mainloop()
"lively.exe"
"Playnite.DesktopApp.exe"
"parsecd.exe"
"honeygain.exe"
"everything.exe"